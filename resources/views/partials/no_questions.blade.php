<div class="p-4 text-center bg-white dark:bg-gray-800 rounded-lg shadow">
    <p class="text-gray-500 dark:text-gray-400">
        @if(!empty($selectedTags))
            No questions found for selected tags.
        @else
            No questions available yet.
        @endif
    </p>
</div>
